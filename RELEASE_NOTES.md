## [2.0.0] - 2026-07-19

### Added
- **Weather icon**: the weather screen now shows a 16×16 icon based on the Open-Meteo `weathercode` (sun, cloud/fog, rain, snow, thunder).

### Changed
- **Admin password hidden**: the password field in `/config` now uses a masked `password` input instead of plain text.

### Notes
- Major version bump to 2.0.0 reflects a visible UI/UX improvement.
